const Card = {
	// The styles all Cards have in common
	baseStyle: {},
	// Two variants: rounded and smooth
	variants: {
		rounded: {},
		smooth: {},
	},
	// The default variant value
	defaultProps: {
		variant: 'smooth',
	},
};

export default Card;
