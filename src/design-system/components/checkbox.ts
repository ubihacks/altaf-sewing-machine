const baseStyle = {
	label: {
		color: 'muted',
		fontWeight: 'medium',
	},
};

const sizes = {
	md: {
		label: {
			fontSize: 'sm',
		},
	},
};

const defaultProps = {
	colorScheme: 'teal',
};

export default {
	baseStyle,
	sizes,
	defaultProps,
};
